The project executable is in the Angry_Petz_final.zip.
to run it it download this file, unzip it, then run exe in the folder.

We could not upload the entire project file as it was to large so we instead just uploaded all of the assets used during the process.
